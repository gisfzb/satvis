# SatVis 操作手册

SatVis 是一个卫星轨道可视化和过境预测的 Web 应用。

## 目录

1. [项目概述](#项目概述)
2. [代码结构](#代码结构)
3. [环境配置](#环境配置)
4. [常用命令](#常用命令)
5. [开发指南](#开发指南)
6. [部署指南](#部署指南)

---

## 项目概述

### 功能特性

- **轨道计算**: 根据 TLE（两行轨道根数）计算卫星位置和轨道
- **地面站设置**: 通过地理定位或地图选点设置地面站
- **过境预测**: 计算指定地面站的卫星可见窗口
- **离线支持**: 作为 PWA（渐进式 Web 应用）支持离线使用
- **浏览器通知**: 本地通知提醒卫星过境

### 技术栈

| 类别 | 技术 |
|------|------|
| 前端框架 | Vue 3 + Vite |
| 3D 地球 | CesiumJS |
| 卫星计算 | Satellite.js |
| UI 组件 | PrimeVue |
| 状态管理 | Pinia |
| 国际化 | Vue I18n |
| 后端 | Cloudflare Worker |
| PWA | Workbox |

---

## 代码结构

```
satvis/
├── index.html              # 主入口页面
├── embedded.html           # 嵌入式页面
├── test.html               # 测试页面
│
├── src/                    # 前端源代码
│   ├── App.vue             # 根组件
│   ├── app.js              # 应用入口
│   │
│   ├── components/         # Vue 组件
│   │   ├── Satvis.vue              # 主组件
│   │   ├── SatelliteSelect.vue     # 卫星选择器
│   │   ├── CustomSatellite.vue     # 自定义卫星
│   │   ├── LanguageSwitcher.vue   # 语言切换
│   │   └── VueCesiumController.js # Cesium 控制器
│   │
│   ├── stores/             # Pinia 状态管理
│   │   ├── cesium.js       # Cesium 状态
│   │   └── sat.js          # 卫星数据状态
│   │
│   ├── modules/            # 核心业务模块
│   │   ├── CesiumController.js     # Cesium 地球控制器
│   │   ├── Orbit.js                 # 轨道计算
│   │   ├── SatelliteManager.js      # 卫星管理器
│   │   ├── SatelliteComponentCollection.js  # 卫星可视化组件集合
│   │   ├── SatelliteProperties.js    # 卫星属性
│   │   ├── GroundStationEntity.js    # 地面站实体
│   │   └── util/                     # 工具函数
│   │
│   ├── router/             # Vue Router 配置
│   ├── composables/        # Vue Composables
│   ├── i18n/               # 国际化文件
│   ├── css/                # 样式文件
│   ├── images/             # SVG 图片资源
│   └── config/             # 配置文件
│
├── worker/                 # Cloudflare Worker 后端
│   ├── src/                # Worker 源代码
│   ├── package.json        # Worker 依赖
│   └── wrangler.jsonc      # Wrangler 配置
│
├── data/                   # 静态数据（git submodule）
│   ├── tle/                # TLE 轨道数据
│   └── cesium/             # Cesium 静态资源
│
├── public/                 # 公共静态资源
└── vite.config.js          # Vite 配置
```

### 关键文件说明

| 文件/目录 | 说明 |
|----------|------|
| `src/modules/CesiumController.js` | Cesium 地球初始化和控制核心 |
| `src/modules/SatelliteManager.js` | 卫星数据管理、加载和更新 |
| `src/modules/Orbit.js` | 基于 TLE 的轨道计算 |
| `src/stores/sat.js` | Pinia store，管理卫星列表和选中状态 |
| `src/stores/cesium.js` | Pinia store，管理 Cesium 状态 |

---

## 环境配置

### 1. 克隆项目

```bash
git clone https://github.com/Flowm/satvis.git
cd satvis
```

### 2. 初始化 Git 子模块

```bash
git submodule update --init
```

### 3. 安装依赖

```bash
npm install
```

> **注意**: 如果在中国大陆访问 GitHub 较慢，可使用国内镜像：
> ```bash
> npm config set registry https://registry.npmmirror.com
> ```

### 4. 配置环境变量（可选）

复制 `.env.example` 为 `.env.development` 或 `.env.production`：

```bash
cp .env.example .env.development
```

目前仅支持 PostHog 分析相关配置：
```
VITE_POSTHOG_KEY=your_posthog_key
VITE_POSTHOG_HOST=your_posthog_host
```

---

## 常用命令

### 开发

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器 |
| `npm run start` | 同上，启动开发服务器 |
| `npm run start:anyhost` | 启动开发服务器，允许外部访问 |

### 构建

| 命令 | 说明 |
|------|------|
| `npm run build` | 构建生产版本（输出到 `dist/`） |
| `npm run serve` | 构建并本地预览 |
| `npm run preview` | 预览已构建的版本 |

### 代码质量

| 命令 | 说明 |
|------|------|
| `npm run lint` | 运行完整检查（oxlint + prettier + vue-tsc） |
| `npm run lint:fix` | 自动修复代码风格问题 |
| `npm run type-check` | 仅进行 TypeScript 类型检查 |

### 数据更新

| 命令 | 说明 |
|------|------|
| `npm run update-tle` | 从 CelesTrak/NORAD 获取最新 TLE 数据 |
| `npm run update-custom-data` | 更新 TLE + 自定义数据 |

### 部署

| 命令 | 说明 |
|------|------|
| `npm run deploy` | 构建前端并部署 Worker 到 Cloudflare |

### 清理

| 命令 | 说明 |
|------|------|
| `npm run clean` | 删除 `dist/` 目录 |

---

## 开发指南

### 项目架构

```
┌─────────────────────────────────────────┐
│              Vue 3 App                   │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  │
│  │ Pinia   │  │ Vue     │  │ PrimeVue│  │
│  │ Stores  │  │ Router  │  │   UI    │  │
│  └────┬────┘  └─────────┘  └─────────┘  │
│       │                                    │
│  ┌────▼────────────────────────────────┐  │
│  │         CesiumJS 3D Globe           │  │
│  │  ┌─────────────────────────────────┐ │  │
│  │  │    Satellite Visualization     │ │  │
│  │  │    + Ground Station Marker     │ │  │
│  │  │    + Orbit Paths                │ │  │
│  │  └─────────────────────────────────┘ │  │
│  └──────────────────────────────────────┘  │
└─────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────┐
│         Cloudflare Worker API            │
│  (可选: 后端数据服务)                     │
└─────────────────────────────────────────┘
```

### 添加新组件

1. 在 `src/components/` 创建 Vue 文件（使用 PascalCase 或 kebab-case）
2. 组件名在模板中使用 kebab-case
3. 使用 Pinia store 管理共享状态

### 添加新页面

1. 在 `src/` 下创建新页面组件
2. 在 `src/router/` 配置路由
3. 更新入口 HTML 文件（如需独立入口）

### Cesium 资源

- Cesium 静态资源（地形、纹理、星图等）通过 `vite-plugin-static-copy` 自动复制到 `dist/cesium/`
- `CESIUM_BASE_URL` 在 `vite.config.js` 中定义为 `"./cesium"`

### 路径别名

| 别名 | 实际路径 |
|------|---------|
| `@/*` | `src/*` |
| `@components/*` | `src/components/*` |
| `@lib/*` | `src/lib/*` |

---

## 部署指南

### 前端部署

项目构建后生成 `dist/` 目录，包含：
- 静态 HTML/CSS/JS 文件
- Cesium 静态资源
- TLE 数据文件

可部署到任意静态托管服务：
- Cloudflare Pages
- Vercel
- GitHub Pages
- 任意 HTTP 服务器

### Cloudflare Worker 部署

Worker 目录 (`worker/`) 需要单独安装依赖：

```bash
cd worker
npm install
```

部署 Worker：
```bash
cd worker
npm run deploy
```

或使用项目根目录的快捷命令：
```bash
npm run deploy
```

### PWA 配置

应用使用 `vite-plugin-pwa` 配置 Service Worker，实现：
- 静态资源缓存
- 离线可用
- 自动更新

---

## 常见问题

### Q: 构建失败，提示找不到 @cesium 模块

**A**: 确保已运行 `git submodule update --init` 并成功拉取子模块内容。

### Q: Cesium 地图不显示

**A**: 检查 `dist/cesium/` 目录是否存在且包含完整资源，以及 `CESIUM_BASE_URL` 配置是否正确。

### Q: TLE 数据过时

**A**: 运行 `npm run update-tle` 从 CelesTrak 获取最新数据。

### Q: 开发时端口被占用

**A**: Vite 默认使用端口 5173，可通过 `--port` 参数指定其他端口：
```bash
npm run dev -- --port 3000
```

---

## License

MIT License - 详见 `LICENSE` 文件。
